{
  "name": "boeuf-tracker-web",
  "version": "2.0.0",
  "description": "Serveur web Bun + Hono pour Boeuf Tracker (proxy vers le worker Python)",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
